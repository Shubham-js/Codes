#include<bits/stdc++.h>
using namespace std;
int main() {
	vector<int>v[5];
	v[0].push_back(1);
	v[0].push_back(1);
	v[0].push_back(1);
	v[0].push_back(1);
	v[0].push_back(1);
	v[0].push_back(1);
	v[0].push_back(1);
	v[0].push_back(1);
	for (int i = 0; i < v[0].size(); i++)
		cout << v[0][i] << endl;
	return 0;
}